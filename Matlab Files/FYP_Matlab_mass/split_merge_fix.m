function WAYPOINT = split_merge_fix(PATH)
    path_end_idx = size(PATH, 1);
    path_beg_idx = 1;
    LINES_NUM = 1;
    
    WAYPOINT = [PATH(1, :); PATH(2, :)];
    
    while 1
        
        M_C_Matrix = zeros(LINES_NUM, 2);
        
        for i = 1:LINES_NUM
            
        end
        
        
    end
    
end
#include "slam_lib.h"

void SLAM::update_map(){
	cout<< point_cloud.data.size()<< endl;
	/*
	int temp_length = point_cloud.width -1;

	if(PC_update < temp_length){
		for(int i = PC_update+1; i <= temp_length; i++){
			if(abs(point_cloud.data[PC_update][2] - __height_target) < HEIGHT_RANGE){

			}
		}
		


		auto it = find(local_map.begin(), local_map.end(), );
		if(it == set.end()){
			return 0;
		}else{
			return 1;
		}
		if
	}
	*/
}